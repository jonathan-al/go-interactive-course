# Exercise 2: Multiple Return Values

Write a function called `divide` that takes two `int` parameters (`a` and `b`) and returns two values: the quotient and the remainder.

In `main`, call `divide(10, 3)` and print the result using this format:

```
10 / 3 = 3 remainder 1
```
