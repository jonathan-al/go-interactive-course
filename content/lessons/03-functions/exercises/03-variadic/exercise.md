# Exercise 3: Variadic Functions

Write a variadic function called `sum` that accepts any number of `int` arguments and returns their total.

In `main`, call `sum(1, 2, 3, 4, 5)` and print the result using this format:

```
Sum of [1 2 3 4 5] = 15
```
