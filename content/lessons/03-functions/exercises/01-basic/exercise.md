# Exercise 1: Basic Functions

Write a function called `add` that takes two `int` parameters and returns their sum.

In `main`, call `add(5, 3)` and print the result using this format:

```
5 + 3 = 8
```
