# Exercise 4: Closures

Write a function called `counter` that returns a closure (a `func() int`). Each time the returned closure is called, it should increment and return an internal count starting from 1.

In `main`, create a counter and call it 3 times, printing each result:

```
1
2
3
```
